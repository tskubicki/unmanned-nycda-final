<h2>Top pilots</h2>
<% @global_leaderboard.each do |u| %>
	<p><%= image_tag u.avatar.url(:thumb) %> <%= u.username %> <%= u.total %></p>
<% end %>

<h2>Friends leaderboard</h2>
	<% @friends_leaderboard.each do |u| %>
		<p><%= image_tag u.avatar.url(:thumb) %> <%= u.username %> <%= u.total %></p>
	<% end %>